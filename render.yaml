services:
  - type: web
    name: api-secti-backend
    runtime: node
    region: ohio
    plan: starter
    buildCommand: cd backend && npm install --legacy-peer-deps && test -f node_modules/zod/package.json && npm run migrations || true
    startCommand: cd backend && node --import tsx/esm src/server.ts
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: '20'
