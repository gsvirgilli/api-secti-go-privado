services:
  - type: web
    name: api-secti-backend
    runtime: node
    region: ohio
    plan: starter
    buildCommand: cd backend && npm ci --legacy-peer-deps && npm run build
    startCommand: cd backend && npm run migrations || true && node --import tsx/esm src/server.ts
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: '20'
