services:
  - type: web
    name: api-secti-backend
    runtime: node
    region: ohio
    plan: starter
    buildCommand: cd backend && rm -rf node_modules package-lock.json && npm install --prefer-offline --no-audit && npm run build
    startCommand: cd backend && npm run migrations || true && node --import tsx/esm src/server.ts
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: '20'
